const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const students = await prisma.student.findMany({ where: { status: 'active', practiceStatus: { not: 'completed' }, OR: [ { instructorId: null }, { practiceGroupId: null } ] }, select: { groupId: true } }); const groupIds = [...new Set(students.map(s => s.groupId))]; console.log(groupIds); } main().finally(() => prisma.$disconnect());
