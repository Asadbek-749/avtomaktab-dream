const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const allStudents = await prisma.student.findMany(); const tGroups = await prisma.group.findMany(); const group = await prisma.practiceGroup.findFirst({ where: { name: 'bbb222' } }); for (const tGroup of tGroups) { const count = allStudents.filter(s => { if (s.groupId !== tGroup.id) return false; const isEligible = !s.practiceGroupId && s.status !== 'completed' && s.practiceStatus !== 'completed'; return isEligible; }).length; console.log(tGroup.name, count); } } main().finally(() => prisma.$disconnect());
