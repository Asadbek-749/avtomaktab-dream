const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const group = await prisma.group.findFirst({ where: { name: 'BC 1-guruh' } }); const students = await prisma.student.findMany({ where: { groupId: group.id } }); console.log(students.map(s => ({ id: s.id, name: s.firstName, practiceGroupId: s.practiceGroupId, status: s.status, practiceStatus: s.practiceStatus }))); } main().finally(() => prisma.$disconnect());
