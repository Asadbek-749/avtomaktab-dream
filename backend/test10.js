const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const students = await prisma.student.findMany({ where: { status: 'completed' } }); console.log('Students with theory completed:', students.length); const eligibleForPractice = students.filter(s => !s.practiceGroupId && s.practiceStatus !== 'completed'); console.log('Eligible for practice:', eligibleForPractice.length); } main().finally(() => prisma.$disconnect());
