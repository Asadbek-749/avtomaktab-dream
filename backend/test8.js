const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const group = await prisma.group.findFirst({ where: { name: 'Avto-Test-102' } }); const students = await prisma.student.findMany({ where: { groupId: group.id } }); console.log(students.map(s => ({ id: s.id, name: s.firstName, instructorId: s.instructorId, practiceGroupId: s.practiceGroupId, status: s.status, practiceStatus: s.practiceStatus }))); } main().finally(() => prisma.$disconnect());
