const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const students = await prisma.student.findMany({ where: { instructorId: '1643234b-7bf3-4485-827b-9a2affd73a43' } }); console.log('Total assigned to Asadbek:', students.length); console.log('With practice group:', students.filter(s => s.practiceGroupId).length); } main().finally(() => prisma.$disconnect());
