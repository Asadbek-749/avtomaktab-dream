const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const allStudents = await prisma.student.findMany(); const group = await prisma.practiceGroup.findFirst({ where: { name: 'bbb222' } }); console.log('Practice group:', group); const filtered = allStudents.filter(s => { const isUnassigned = !s.instructorId; const isAssignedButNoGroup = s.instructorId === group.instructorId && !s.practiceGroupId; const isActive = s.status !== 'completed' && s.practiceStatus !== 'completed'; return (isUnassigned || isAssignedButNoGroup) && isActive; }); console.log('Eligible students count:', filtered.length); } main().finally(() => prisma.$disconnect());
