const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const allStudents = await prisma.student.findMany(); const tGroups = await prisma.group.findMany(); for (const tGroup of tGroups) { const count = allStudents.filter(s => s.groupId === tGroup.id).length; console.log(tGroup.name, count); } } main().finally(() => prisma.$disconnect());
